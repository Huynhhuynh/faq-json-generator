/**
 * @package FAQ Json Generator
 *
 * @since 1.0.0
 * @author Beplus
 */

import Vue from 'vue';
import App from './components/app.vue';

var app = new Vue({
   el: '#app',
   render: h => h(App),
})
